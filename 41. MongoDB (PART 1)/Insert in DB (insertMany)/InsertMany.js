/**
 * INSERT in DB (inserMany())
------------------------------
insertMany(array of documents)

example:
    db.student.insertMany([{name: "bob", marks: 65}, {name: "eve", city: "Delhi"}])

*Insert multiple documents into a collection.



 */