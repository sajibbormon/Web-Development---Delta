/*
Http Headers
-------------

HTTP headers are key-value pairs included in the header section of HTTP requests and responses,
providing metadata about the message. They convey information about the client, server, requested 
resource, or the message body itself. 

header, value pair.

It request/response with additional data.

metadata means data (additional information) about data.


https://icanhazdadjoke.com/ API has accept header.
Accept header generaly tells how a response should be accept.

accept format:
    text/html
    application/json
    text/plain

*/