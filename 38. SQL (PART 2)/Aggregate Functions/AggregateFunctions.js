/**
 * Aggregate Functions
------------------------
Aggregate functions perform a calculation on a set of values, and retrun a single value.

1. COUNT()

2. MAX()

3. MIN()

4. SUM()

6. AVG()



Example:

SELECT MAX(col_name)
FROM table_name;

SELECT MAX(marks)
FROM student;






 */