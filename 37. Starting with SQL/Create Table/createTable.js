/**
 * Table Queries
------------------

1. Create (to create a table)

2. Insert (we add data(tuples) to the table)

3. Update (we update data of a table)

4. Alter (alter the schema means alter the column)

5. Truncate  (empty the table)

6. Delete   (delete  the whole table)



1. Create  Table:
-----------------

Create table means define Schema/columns of a table.

CREATE TABLE table_name(
    column_name1 datatype constraint,
    column_name2 datatype constraints
);



Data types screenshot is saved. (read from screenshot)

More,

TINYINT          -128 to 127

but if we use TINYINT UNSIGNED then the range  will be 0 to 255 bcz we are not considering the 
negatives.




 */